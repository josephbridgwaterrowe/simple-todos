{
  "name": "simple-todos",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "chimp-test": "chimp --mocha --path=tests"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
